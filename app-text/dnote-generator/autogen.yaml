dnote-generator_rule:
  defaults:
    cat: app-text
    user: dnote
    repo: dnote
  packages:
    - dnote:
        buildargs: -tags="fts5" ./pkg/cli
        tagprefix: cli-v
        bin: cli
    - dnote-server:
        buildargs: ./pkg/server
        tagprefix: server-v
        bin: server